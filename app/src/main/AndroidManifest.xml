<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.questiongame">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Controller.Activity.SettingListActivity"></activity>
        <activity android:name=".Controller.Activity.QuestionListActivity" />
        <activity android:name=".Controller.Activity.SignInActivity" />
        <activity android:name=".Controller.Activity.QuestionGameActivity" />
        <activity android:name=".Controller.Activity.Setting" />
        <activity android:name=".Controller.Activity.CheatActivity" />
        <activity
            android:name=".Controller.Activity.LoginActivity"
            android:icon="@drawable/ic_launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>